import { BIOFLOW_ADMIN_CLINICS_PATH } from '../../../constants/paths'
import { ClinicsAll } from '..'

export default [
  {
    name: 'ClinicsAll',
    path: BIOFLOW_ADMIN_CLINICS_PATH,
    exact: true,
    component: ClinicsAll
  }
]
