import { BIOFLOW_PATH } from '../../../constants/paths'

import { Dashboard } from '..'

export default [
  {
    name: 'Dashboard',
    path: BIOFLOW_PATH,
    exact: false,
    component: Dashboard
  }
]
