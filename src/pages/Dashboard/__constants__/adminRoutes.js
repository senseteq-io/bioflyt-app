import { BIOFLOW_ADMIN_PATH } from '../../../constants/paths'

import { Dashboard } from '..'

export default [
  {
    name: 'Dashboard',
    path: BIOFLOW_ADMIN_PATH,
    exact: false,
    component: Dashboard
  }
]
